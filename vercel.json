{
  "builds": [
    {
      "src": "web/api/*.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/web/api/$1.py"
    }
  ],
  "buildCommand": "cd web-app && npm install && npm run build",
  "outputDirectory": "web-app/dist"
}
